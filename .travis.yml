language: generic
sudo: required
services:
  - docker

before_install:
  - docker build  -t krishnkamal/docker-react -f Dockerfile.dev .

script:
  - docker run krishnkamal/docker-react npm run test -- --coverage
